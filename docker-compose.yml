services:
  dockerhq:
    build: .
    container_name: dockerhq
    ports:
      - "3500:8000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
